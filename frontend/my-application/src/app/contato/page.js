import { Button } from "@/components/ui/button";

export default function Contato() {
    return (
        <div className="flex flex-col justify-center min-h-screen ">
            <Button className="underline">Voltar Home</Button>

        </div>

    )
}